interface:
  display_name: Atom Write Product Requirements
  short_description: Route and apply Atom Write Product Requirements
  default_prompt: Use $atom-write-product-requirements to handle this request
    within its declared capability boundary.
policy:
  allow_implicit_invocation: true
