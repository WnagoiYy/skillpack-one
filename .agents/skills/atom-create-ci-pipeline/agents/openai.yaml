interface:
  display_name: Atom Create Ci Pipeline
  short_description: Route and apply Atom Create Ci Pipeline
  default_prompt: Use $atom-create-ci-pipeline to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
