interface:
  display_name: Category Skill Agent Governance
  short_description: Route and apply Category Skill Agent Governance
  default_prompt: Use $category-skill-agent-governance to handle this request
    within its declared capability boundary.
policy:
  allow_implicit_invocation: true
