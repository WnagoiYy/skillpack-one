interface:
  display_name: Atom Extract Document Data
  short_description: Route and apply Atom Extract Document Data
  default_prompt: Use $atom-extract-document-data to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
