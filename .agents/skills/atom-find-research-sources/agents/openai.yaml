interface:
  display_name: Atom Find Research Sources
  short_description: Route and apply Atom Find Research Sources
  default_prompt: Use $atom-find-research-sources to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
