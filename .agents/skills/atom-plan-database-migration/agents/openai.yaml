interface:
  display_name: Atom Plan Database Migration
  short_description: Route and apply Atom Plan Database Migration
  default_prompt: Use $atom-plan-database-migration to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
