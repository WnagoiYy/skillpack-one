interface:
  display_name: Atom Plan Content Strategy
  short_description: Route and apply Atom Plan Content Strategy
  default_prompt: Use $atom-plan-content-strategy to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
