interface:
  display_name: Atom Plan Cloud Deployment
  short_description: Route and apply Atom Plan Cloud Deployment
  default_prompt: Use $atom-plan-cloud-deployment to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
