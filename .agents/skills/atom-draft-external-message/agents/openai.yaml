interface:
  display_name: Atom Draft External Message
  short_description: Route and apply Atom Draft External Message
  default_prompt: Use $atom-draft-external-message to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
