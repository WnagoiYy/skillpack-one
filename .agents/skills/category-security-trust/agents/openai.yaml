interface:
  display_name: Category Security Trust
  short_description: Route and apply Category Security Trust
  default_prompt: Use $category-security-trust to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
