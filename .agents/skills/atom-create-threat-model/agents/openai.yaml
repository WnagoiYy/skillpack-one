interface:
  display_name: Atom Create Threat Model
  short_description: Route and apply Atom Create Threat Model
  default_prompt: Use $atom-create-threat-model to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
