interface:
  display_name: Atom Design Pricing Strategy
  short_description: Route and apply Atom Design Pricing Strategy
  default_prompt: Use $atom-design-pricing-strategy to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
