interface:
  display_name: Atom Audit Dependencies
  short_description: Route and apply Atom Audit Dependencies
  default_prompt: Use $atom-audit-dependencies to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
