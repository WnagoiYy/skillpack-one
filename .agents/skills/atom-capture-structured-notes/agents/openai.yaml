interface:
  display_name: Atom Capture Structured Notes
  short_description: Route and apply Atom Capture Structured Notes
  default_prompt: Use $atom-capture-structured-notes to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
