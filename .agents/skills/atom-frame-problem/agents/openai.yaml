interface:
  display_name: Atom Frame Problem
  short_description: Route and apply Atom Frame Problem
  default_prompt: Use $atom-frame-problem to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
