interface:
  display_name: Atom Plan Code Change
  short_description: Route and apply Atom Plan Code Change
  default_prompt: Use $atom-plan-code-change to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
