interface:
  display_name: Atom Generate Image
  short_description: Route and apply Atom Generate Image
  default_prompt: Use $atom-generate-image to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
