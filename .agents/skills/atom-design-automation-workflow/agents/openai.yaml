interface:
  display_name: Atom Design Automation Workflow
  short_description: Route and apply Atom Design Automation Workflow
  default_prompt: Use $atom-design-automation-workflow to handle this request
    within its declared capability boundary.
policy:
  allow_implicit_invocation: true
