interface:
  display_name: Meta Skill Governor
  short_description: Route and apply Meta Skill Governor
  default_prompt: Use $meta-skill-governor to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
