interface:
  display_name: Atom Create Presentation
  short_description: Route and apply Atom Create Presentation
  default_prompt: Use $atom-create-presentation to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
