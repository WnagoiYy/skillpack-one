interface:
  display_name: Atom Analyze Customer Segments
  short_description: Route and apply Atom Analyze Customer Segments
  default_prompt: Use $atom-analyze-customer-segments to handle this request
    within its declared capability boundary.
policy:
  allow_implicit_invocation: true
