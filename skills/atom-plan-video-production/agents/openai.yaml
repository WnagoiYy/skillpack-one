interface:
  display_name: Atom Plan Video Production
  short_description: Route and apply Atom Plan Video Production
  default_prompt: Use $atom-plan-video-production to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
