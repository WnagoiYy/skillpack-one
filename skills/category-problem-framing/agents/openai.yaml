interface:
  display_name: Category Problem Framing
  short_description: Route and apply Category Problem Framing
  default_prompt: Use $category-problem-framing to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
