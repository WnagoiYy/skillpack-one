interface:
  display_name: Atom Validate Data Pipeline
  short_description: Route and apply Atom Validate Data Pipeline
  default_prompt: Use $atom-validate-data-pipeline to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
