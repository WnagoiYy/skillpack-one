interface:
  display_name: Atom Design Database Schema
  short_description: Route and apply Atom Design Database Schema
  default_prompt: Use $atom-design-database-schema to handle this request within
    its declared capability boundary.
policy:
  allow_implicit_invocation: true
