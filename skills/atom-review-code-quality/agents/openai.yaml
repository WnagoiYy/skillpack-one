interface:
  display_name: Atom Review Code Quality
  short_description: Route and apply Atom Review Code Quality
  default_prompt: Use $atom-review-code-quality to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
