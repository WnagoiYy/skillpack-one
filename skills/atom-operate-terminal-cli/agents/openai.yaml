interface:
  display_name: Atom Operate Terminal Cli
  short_description: Route and apply Atom Operate Terminal Cli
  default_prompt: Use $atom-operate-terminal-cli to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
