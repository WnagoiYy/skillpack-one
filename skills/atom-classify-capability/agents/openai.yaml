interface:
  display_name: Atom Classify Capability
  short_description: Route and apply Atom Classify Capability
  default_prompt: Use $atom-classify-capability to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
