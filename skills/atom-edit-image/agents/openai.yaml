interface:
  display_name: Atom Edit Image
  short_description: Route and apply Atom Edit Image
  default_prompt: Use $atom-edit-image to handle this request within its declared
    capability boundary.
policy:
  allow_implicit_invocation: true
