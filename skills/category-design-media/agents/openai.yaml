interface:
  display_name: Category Design Media
  short_description: Route and apply Category Design Media
  default_prompt: Use $category-design-media to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
