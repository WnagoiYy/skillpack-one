interface:
  display_name: Category Code Quality Delivery
  short_description: Route and apply Category Code Quality Delivery
  default_prompt: Use $category-code-quality-delivery to handle this request
    within its declared capability boundary.
policy:
  allow_implicit_invocation: true
