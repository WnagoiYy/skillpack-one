interface:
  display_name: Category Software Use
  short_description: Route and apply Category Software Use
  default_prompt: Use $category-software-use to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
