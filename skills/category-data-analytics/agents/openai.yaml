interface:
  display_name: Category Data Analytics
  short_description: Route and apply Category Data Analytics
  default_prompt: Use $category-data-analytics to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
