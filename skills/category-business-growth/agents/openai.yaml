interface:
  display_name: Category Business Growth
  short_description: Route and apply Category Business Growth
  default_prompt: Use $category-business-growth to handle this request within its
    declared capability boundary.
policy:
  allow_implicit_invocation: true
