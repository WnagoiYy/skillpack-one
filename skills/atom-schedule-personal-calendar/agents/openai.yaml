interface:
  display_name: Atom Schedule Personal Calendar
  short_description: Route and apply Atom Schedule Personal Calendar
  default_prompt: Use $atom-schedule-personal-calendar to handle this request
    within its declared capability boundary.
policy:
  allow_implicit_invocation: true
