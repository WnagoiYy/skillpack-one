# 发布策略

版本遵循语义化版本。研究阶段使用 `vMAJOR.MINOR.PATCH-alpha.N`，在真实任务完成率证据获得认证前始终标记为 GitHub 预发布。

发布必须满足：工作区干净、本地与远端 revision 精确一致、该 revision 的 GitHub Actions 无警告通过、独立路由门禁通过、生成投影为最新、官方 Skill/插件校验通过、依赖与秘密扫描通过、来源归属完整，并创建不可变的 annotated tag。已发布标签永不移动；修正使用新的预发布序号。变更日志必须记录面向用户的行为和已知证据边界。
